{
  "name": "@live-demo-agent/policies",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./generated/typescript/src/index.js",
  "types": "./generated/typescript/src/index.ts",
  "scripts": {
    "validate": "tsx scripts/validate-policies.ts",
    "generate:python": "python3 scripts/generate-python-policies.py",
    "generate:typescript": "tsx scripts/generate-typescript-policies.ts",
    "generate": "pnpm validate && pnpm generate:typescript && pnpm generate:python",
    "lint": "eslint scripts/**/*.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "ajv": "^8.17.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.0.0"
  }
}

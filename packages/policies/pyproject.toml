[project]
name = "live-demo-policies"
version = "0.1.0"
description = "Shared deterministic policy rules for the live demo agent platform."
requires-python = ">=3.12,<3.14"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["generated/python/live_demo_policies"]

# Simple Dashboard Fixture

Local deterministic product surface for safe browser and E2E tests.

```mermaid
flowchart LR
    Dashboard["/ dashboard"] --> Metric["/metrics/new metric form"]
    Dashboard --> Reports["/reports reports"]
```

# Grafana Dashboards

Phase 1 only provisions the folder structure. Dashboards will be added when runtime metrics are emitted in later phases.

Expected future dashboards:

- Session latency.
- Provider latency and error rate.
- Browser action validation and execution latency.
- Voice first-audio latency.
- Learner queue depth.

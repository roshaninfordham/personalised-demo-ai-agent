apiVersion: apps/v1
kind: Deployment
metadata:
  name: browser-runtime
spec:
  template:
    spec:
      containers:
        - name: browser-runtime
          resources:
            requests:
              cpu: "1"
              memory: 2Gi
            limits:
              cpu: "4"
              memory: 4Gi

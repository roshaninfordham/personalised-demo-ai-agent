apiVersion: v1
kind: Service
metadata:
  name: agent-runtime
  labels:
    app.kubernetes.io/name: agent-runtime
spec:
  selector:
    app.kubernetes.io/name: agent-runtime
  ports:
    - name: http
      port: 8300
      targetPort: http

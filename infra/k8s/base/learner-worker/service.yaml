apiVersion: v1
kind: Service
metadata:
  name: learner-worker
  labels:
    app.kubernetes.io/name: learner-worker
spec:
  selector:
    app.kubernetes.io/name: learner-worker
  ports:
    - name: http
      port: 8400
      targetPort: http

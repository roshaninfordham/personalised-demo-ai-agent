apiVersion: v1
kind: Service
metadata:
  name: browser-runtime
  labels:
    app.kubernetes.io/name: browser-runtime
spec:
  selector:
    app.kubernetes.io/name: browser-runtime
  ports:
    - name: http
      port: 8200
      targetPort: http

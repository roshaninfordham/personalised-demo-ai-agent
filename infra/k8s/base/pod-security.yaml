apiVersion: v1
kind: ConfigMap
metadata:
  name: pod-security-notes
data:
  restricted-profile: "Namespace enforces restricted Pod Security admission; browser runtime uses unprivileged emptyDir mounts for writable paths."

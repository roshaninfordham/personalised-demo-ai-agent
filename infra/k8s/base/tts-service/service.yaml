apiVersion: v1
kind: Service
metadata:
  name: tts-service
  labels:
    app.kubernetes.io/name: tts-service
spec:
  selector:
    app.kubernetes.io/name: tts-service
  ports:
    - name: http
      port: 8100
      targetPort: http

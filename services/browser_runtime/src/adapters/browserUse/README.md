browser-use is optional and disabled by default.

Future integrations should run in a separate disposable exploration context and produce discovered screens, candidate actions, route suggestions, and warnings.

It must not execute visible live-demo actions or replace deterministic Playwright action execution in the browser runtime.

